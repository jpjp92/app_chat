
# 🚀 Aura Gemini - Intelligent Real-time AI Messenger

**Aura Gemini**는 Google의 최신 **Gemini 3** 엔진과 **실시간 Google 검색(Grounding)** 기능을 결합한 차세대 지능형 챗봇입니다. 특히 무료 티어의 할당량 제한을 극복하기 위한 **지능형 회복탄력성(Resilience)** 로직이 탑재되어 중단 없는 대화 경험을 제공합니다.

---

## ✨ 핵심 기능 (Key Features)

### 🛡️ 무중단 서비스 아키텍처 (Uninterrupted Service)
- **멀티 API 키 로테이션**: `API_KEY`와 `API_KEY2`를 동시에 지원합니다. 첫 번째 키의 할당량이 소진되면 자동으로 두 번째 키로 전환하여 서비스를 지속합니다.
- **지능형 모델 폴백 (Model Fallback)**: 특정 모델(예: Gemini 3 Flash)의 일일 호출 한도(RPD)가 초과될 경우, 즉시 하위 모델(`gemini-2.5-flash`, `gemini-flash-latest`)로 자동 전환하여 응답 실패를 방지합니다.
- **스마트 에러 핸들링**: 복잡한 API 에러 코드를 분석하여 사용자에게 "할당량 초과" 또는 "인증 오류"와 같은 친절한 한국어 메시지로 변환하여 안내합니다.

### 🔍 실시간 Google 검색 (Search Grounding)
- **최신 정보 반영**: 학습 데이터에 의존하지 않고, 현재 시점의 뉴스, 주가, 날씨 등을 Google 검색을 통해 실시간으로 확인합니다.
- **투명한 출처 공개**: AI가 참고한 실제 웹사이트의 제목과 링크를 카드 형태로 제공하여 정보의 신뢰성을 보장합니다.

### 📄 지능형 멀티모달 분석 (Multi-modal)
- **PDF 딥러닝 분석**: 최대 10MB의 PDF 파일을 업로드하여 리포트 요약, 데이터 추출, 논리적 해석이 가능합니다.
- **시각 지능**: 이미지를 분석하여 상황을 설명하거나 사진 속 텍스트를 인식합니다.

### 🎙️ 직관적인 음성 인터페이스 (STT/TTS)
- **연속 음성 인식 (STT)**: 마이크를 통해 자연스럽게 말하면 텍스트로 변환되며, 대화 종료 시 자동 전송 기능을 지원합니다.
- **고품질 음성 출력 (TTS)**: Gemini의 최신 음성 합성 엔진을 통해 답변을 자연스러운 목소리로 읽어줍니다.

---

## 🛠️ 배포 및 설정 (Deployment)

이 프로젝트는 **Vercel** 환경에 최적화되어 있습니다.

### 1. API 키 준비
- [Google AI Studio](https://aistudio.google.com/app/apikey)에서 하나 이상의 API 키를 발급받으세요. (여러 프로젝트에서 키를 생성하면 더 많은 할당량을 확보할 수 있습니다.)

### 2. Vercel 환경 변수 설정
프로젝트 설정의 **Environment Variables** 섹션에 아래 변수들을 추가하세요.
- **Key**: `API_KEY` / **Value**: `첫 번째 Gemini API Key`
- **Key**: `API_KEY2` / **Value**: `백업용 Gemini API Key (권장)`

> **중요**: 환경 변수 추가 후에는 반드시 Vercel에서 **Redeploy**를 진행해야 설정이 반영됩니다.

---

## 🏗️ 기술 스택 (Tech Stack)

- **Core**: React 19, TypeScript, Vite
- **AI Engine**: 
  - `gemini-3-flash-preview` (메인 모델)
  - `gemini-2.5-flash` (폴백 모델 1)
  - `gemini-flash-latest` (폴백 모델 2)
  - `gemini-2.5-flash-preview-tts` (음성 합성)
- **Styling**: Tailwind CSS, Glassmorphism UX
- **Integrations**: Google Search Grounding, Jina AI Reader

---

## 💡 개발자 참고 사항
- **회복탄력성 로직**: `services/geminiService.ts` 내의 `runWithFailover` 함수가 키 로테이션과 모델 전환을 관리합니다.
- **보안**: API 키는 서버측 환경 변수(process.env)를 통해 안전하게 주입되며, 클라이언트 코드에 직접 노출되지 않도록 Vite 설정을 거칩니다.

---
Developed with ❤️ by **jpjp92**  
*Powered by Google Gemini Next-Gen Intelligence*
